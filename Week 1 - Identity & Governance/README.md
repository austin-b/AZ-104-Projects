Scenario: Build the identity foundation for a fictional company. Create Entra ID users and groups, assign custom RBAC roles at the resource group scope, and enforce a tagging requirement via Azure Policy.

Learn:
- Terraform variables and file structure.